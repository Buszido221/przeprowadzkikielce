---
import Layout from '../../layouts/Layout.astro';
import SubpageHero from '../../components/SubpageHero.astro';
import SectionHeading from '../../components/SectionHeading.astro';
import { site } from '../../data/site';
import { gabarytyPage as page } from '../../data/services';

const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Strona główna', item: site.domain },
        { '@type': 'ListItem', position: 2, name: 'Transport specjalistyczny', item: `${site.domain}transport-specjalistyczny/` },
        { '@type': 'ListItem', position: 3, name: 'Transport gabarytowy', item: page.seo.canonical },
      ],
    },
    {
      '@type': 'Service',
      name: 'Transport gabarytowy Kielce',
      description: page.seo.description,
      provider: {
        '@type': 'LocalBusiness',
        name: site.brand,
        telephone: site.phone,
        address: {
          '@type': 'PostalAddress',
          streetAddress: site.address.street,
          addressLocality: site.address.city,
          postalCode: site.address.postalCode,
          addressCountry: 'PL',
        },
      },
      areaServed: { '@type': 'State', name: 'województwo świętokrzyskie' },
    },
    {
      '@type': 'FAQPage',
      mainEntity: page.faq.map((q) => ({
        '@type': 'Question',
        name: q.question,
        acceptedAnswer: { '@type': 'Answer', text: q.answer },
      })),
    },
  ],
};
---

<Layout title={page.seo.title} description={page.seo.description} robots="index, follow">
  <link slot="head" rel="canonical" href={page.seo.canonical} />
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

  <SubpageHero
    heading={page.hero.heading}
    lead={page.hero.lead}
    breadcrumbs={page.breadcrumbs}
    ctaLabel={page.hero.ctaLabel}
    note={page.hero.note}
    heroImage={page.hero.image}
  />

  <!-- EXAMPLES -->
  <section class="section section--alt">
    <div class="container">
      <SectionHeading label={page.examples.eyebrow} title={page.examples.heading} />
      <ul class="simple-list">
        {page.examples.items.map((item) => (
          <li class="simple-list__item">{item}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- ASSESSMENT (3 columns) -->
  <section class="section">
    <div class="container">
      <SectionHeading label={page.assessment.eyebrow} title={page.assessment.heading} />
      <div class="assessment-grid">
        {page.assessment.groups.map((group) => (
          <div class="assessment-card">
            <h3 class="assessment-card__title">{group.title}</h3>
            <ul class="assessment-card__list">
              {group.items.map((item) => (
                <li>{item}</li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ESCALERA -->
  <section class="section section--alt">
    <div class="container">
      <SectionHeading label={page.escalera.eyebrow} title={page.escalera.heading} description={page.escalera.text} />
      <div class="equipment-notice">{page.escalera.notice}</div>
    </div>
  </section>

  <!-- PHOTOS -->
  <section class="section">
    <div class="container">
      <SectionHeading label={page.photos.eyebrow} title={page.photos.heading} />
      <div class="photo-grid photo-grid--3">
        {page.photos.items.map((photo) => (
          <div class="photo-grid__item">
            <img src={photo.src} alt={photo.alt} width="600" height="450" loading="lazy" decoding="async" />
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- EXTRA EQUIPMENT -->
  <section class="section section--alt">
    <div class="container">
      <SectionHeading label={page.extraEquipment.eyebrow} title={page.extraEquipment.heading} description={page.extraEquipment.text} />
      <ul class="simple-list">
        {page.extraEquipment.items.map((item) => (
          <li class="simple-list__item">{item}</li>
        ))}
      </ul>
      <p class="notice-box">{page.extraEquipment.note}</p>
    </div>
  </section>

  <!-- INQUIRY -->
  <section class="section">
    <div class="container">
      <SectionHeading label={page.inquiry.eyebrow} title={page.inquiry.heading} />
      <ul class="simple-list">
        {page.inquiry.items.map((item) => (
          <li class="simple-list__item">{item}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- QUICK VALUATION LINKS -->
  <section class="section section--sm">
    <div class="container">
      <SectionHeading label="Szybka wycena" title="Wyceń konkretną usługę" />
      <div class="related-links">
        <a href="/lp/transport-gabarytowy-kielce/" class="arrow-link">Wyceń transport gabarytowy</a>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section section--alt">
    <div class="container">
      <SectionHeading label="Najczęstsze pytania" title="Odpowiedzi na pytania o transport gabarytowy" />
      <div class="faq-list">
        {page.faq.map((item) => (
          <details class="faq-item">
            <summary class="faq-item__question">{item.question}</summary>
            <div class="faq-item__answer">
              <p>{item.answer}</p>
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- FINAL CTA -->
  <section class="final-cta">
    <div class="container">
      <div class="final-cta__inner">
        <h2 class="final-cta__heading">{page.finalCta.heading}</h2>
        <p class="final-cta__text">{page.finalCta.text}</p>
        <div class="final-cta__actions">
          <a href="/wycena/" class="btn btn--primary btn--lg">{page.finalCta.ctaLabel}</a>
          <a href={`tel:${site.phoneRaw}`} class="btn btn--secondary btn--lg">Zadzwoń: 720 719 022</a>
        </div>
        <p class="final-cta__hours">Pn–Pt: {site.hours.weekdays} &middot; Sob: {site.hours.saturday}</p>
      </div>
    </div>
  </section>

  <!-- BACK LINK -->
  <section class="section--sm">
    <div class="container">
      <a href="/transport-specjalistyczny/" class="back-link">Transport specjalistyczny</a>
    </div>
  </section>
</Layout>
