---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ConsentManager from '../components/ConsentManager.astro';
import GtmHead from '../components/GtmHead.astro';
import CampaignAttribution from '../components/CampaignAttribution.astro';
import ClientEvents from '../components/ClientEvents.astro';
import { site } from '../data/site';
import { isStaging, measurementEnabled } from '../lib/environment';
import { getPageAnalyticsContext } from '../lib/page-analytics';
import '../styles/base.css';
import '../styles/main.css';

interface Props {
  title: string;
  description?: string;
  robots?: string;
  ogImage?: string;
  canonical?: string;
}

const {
  title,
  description = 'WHM Przeprowadzki - profesjonalne przeprowadzki w Kielcach i województwie świętokrzyskim.',
  robots = 'noindex, follow',
  ogImage = '/images/hero/hero-1.webp',
  canonical,
} = Astro.props;

const robotsContent = isStaging ? 'noindex, nofollow, noarchive' : robots;
const canonicalUrl = canonical ?? new URL(Astro.url.pathname, site.domain).href;
const ogImageUrl = new URL(ogImage, site.domain).href;
const pageAnalytics = getPageAnalyticsContext(Astro.url.pathname);
---

<!doctype html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content={robotsContent} />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/images/LOGO_WHM.svg" />
    {canonical && <link rel="canonical" href={canonicalUrl} />}
    <title>{title}</title>

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="pl_PL" />
    <meta property="og:site_name" content={site.brand} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1920" />
    <meta property="og:image:height" content="1080" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />

    <GtmHead />
    <CampaignAttribution />
    <ClientEvents />
    <slot name="head" />
  </head>
  <body data-service-type={pageAnalytics.serviceType} data-lead-context={pageAnalytics.leadContext}>
    <a href="#main-content" class="skip-link">Przejdź do treści</a>
    <Header />
    <main id="main-content">
      <slot />
    </main>
    <Footer />
    {measurementEnabled && <ConsentManager />}
  </body>
</html>
